schema_version = 1
project = "gotth-oidc"
profile = "strict"
active = "github-distribution"

[settings]
worktree_root = "worktrees"
workflow_root = "workflow"
default_branch = "main"

[toolchain]
build = "go build ./..."
test = "go test -mod=readonly -race -cover ./..."
lint = "go vet -mod=readonly ./..."
format = "gofmt -w"
coverage = "go test -mod=readonly -coverprofile=coverage.out ./..."

[coverage]
map = "workflow/artifacts/global-coverage-map.md"
profile = "strict"
block_unresolved_high_risk = true
exception_expiry_days = 14

[feature.extraction]
id = "extraction"
slug = "extraction"
name = "Extract hardened OIDC relying-party core"
namespace = "oidc"
parent = ""
state = "done"
risk = "high"
assurance = "traceable-tests"
created_at = "2026-09-02T23:20:00Z"
activated_at = "2026-09-02T23:20:00Z"
updated_at = "2026-09-02T23:34:00Z"
completed_at = "2026-09-02T23:34:00Z"
folder = "workflow/features/extraction"
worktree = "."
branch = "main"
scope = ["Extract protocol and cryptographic mechanics behind a product-neutral API"]
non_goals = ["Move application accounts, sessions, roles, persistence, routes, or provider configuration"]
affected_files = ["**"]
invariants = ["No OAuth token escapes the package", "All provider network endpoints remain on the exact issuer origin"]
acceptance = ["Adversarial unit, race, vet, format, and coverage gates pass"]

[feature.extraction.rollback]
strategy = "delete_unpublished_repository"
notes = "Consumers retain in-tree code until they pin an admitted module version"
data_loss_risk = false
requires_migration_down = false

[[feature.extraction.verification]]
id = "verify"
kind = "unit"
command = "make verify"
required = true
timeout_seconds = 600
side_effect = "local_write"
network = false
retries = 0

[feature.reusable-v0-admission]
id = "reusable-v0-admission"
slug = "reusable-v0-admission"
name = "Admit the reusable hardened OIDC contract"
namespace = "oidc"
parent = ""
state = "done"
risk = "critical"
assurance = "traceable-tests"
created_at = "2026-09-03T01:12:54Z"
activated_at = "2026-09-03T01:12:54Z"
updated_at = "2026-09-03T01:18:32Z"
completed_at = "2026-09-03T01:18:32Z"
folder = "workflow/features/reusable-v0-admission"
worktree = "."
branch = "feature/reusable-v0-admission"
scope = ["Pin the safe signing-algorithm intersection", "Enforce HTTPS or numeric-loopback HTTP", "Prove the exported consumer API and both client types", "Record clean reusable-release evidence"]
non_goals = ["Arbitrary OAuth scopes or API access", "Split-origin providers", "Refresh tokens", "Application persistence, users, sessions, roles, routing, or authorization", "Tag before a consumer pin is approved"]
affected_files = ["README.md", "*.go", "docs/**", "workflow.toml", "workflow/features/reusable-v0-admission/**", "workflow/artifacts/global-coverage-map.md"]
invariants = ["No OAuth token escapes the package", "All provider requests stay on the exact issuer origin", "State, nonce, and PKCE remain independent", "Consumers atomically consume attempts"]
acceptance = ["A mixed provider algorithm list cannot expand verifier algorithms", "Non-loopback HTTP URLs fail before network access", "Public and confidential end-to-end flows and external-package compilation pass", "Race-repeated, clean-clone, and graph gates pass"]

[[feature.reusable-v0-admission.dependency]]
id = "extraction"
type = "prerequisite"
state = "satisfied"
evidence = "workflow/features/extraction/evidence/verification.md"
satisfied_at = "2026-09-03T01:12:54Z"
source_commit = "b21f1c2cb8c3ce2e0a9421efa13cd36e0f6c9c3e"

[feature.reusable-v0-admission.rollback]
strategy = "revert_feature_commit"
notes = "No tag or consumer pin exists; revert the admission commit"
data_loss_risk = false
requires_migration_down = false

[[feature.reusable-v0-admission.interface]]
name = "Go library API"
kind = "api"
stability = "unstable-unreleased"
change = "none"

[[feature.reusable-v0-admission.verification]]
id = "verify"
kind = "unit"
command = "make verify"
required = true
timeout_seconds = 600
side_effect = "local_write"
network = false
retries = 0

[feature.rfc-complete-oidc]
id = "rfc-complete-oidc"
slug = "rfc-complete-oidc"
name = "Complete the reusable OIDC and OAuth standards boundary"
namespace = "oidc"
parent = ""
state = "done"
risk = "critical"
assurance = "traceable-tests"
created_at = "2026-09-03T04:44:11Z"
activated_at = "2026-09-03T04:44:11Z"
updated_at = "2026-09-03T05:31:05Z"
completed_at = "2026-09-03T05:31:05Z"
folder = "workflow/features/rfc-complete-oidc"
worktree = "/home/linus/.openclaw/workspace/.artifacts/worktrees/gotth-oidc-rfc-complete"
branch = "feature/rfc-complete-oidc"
scope = ["Close mandatory OIDC Core, Discovery, OAuth, PKCE, issuer-response, and security-BCP gaps", "Add standardized optional relying-party capabilities behind explicit opt-in contracts", "Preserve the small default Authorization Code plus PKCE path"]
non_goals = ["Application users, sessions, roles, cookies, routing, persistence, or authorization policy", "Operate an identity provider or authorization server", "Silently enable optional token retention, weaker transport, or broader endpoint trust", "Tag before a real consumer pin"]
affected_files = ["README.md", "*.go", "docs/**", "workflow.toml", "workflow/features/rfc-complete-oidc/**", "workflow/artifacts/global-coverage-map.md", "go.mod", "go.sum"]
invariants = ["The default path returns no OAuth token", "Every authorization attempt is bound to issuer, client, redirect URI, and requested validation policy", "Optional network endpoints are discovered, bounded, and independently policy-checked", "Unsafe algorithms and cross-JWT substitution fail closed", "Existing New, Begin, and Complete calls remain source-compatible"]
acceptance = ["Mandatory ID-token audience, authorized-party, token-type, discovery, and mix-up checks pass", "Every standardized optional capability listed in docs/conformance.md has implementation and negative tests or an explicit consumer-boundary justification", "Format, vet, race, repeat-race, fuzz, clean-clone, external-consumer, and graph gates pass"]

[[feature.rfc-complete-oidc.dependency]]
id = "reusable-v0-admission"
type = "prerequisite"
state = "satisfied"
evidence = "workflow/features/reusable-v0-admission/evidence/verification.md"
satisfied_at = "2026-09-03T04:44:11Z"
source_commit = "d7f8398bffc559de83440e9430421178a4503f40"

[feature.rfc-complete-oidc.rollback]
strategy = "revert_feature_commits"
notes = "The repository is untagged and has no consumer pin; main at d7f8398 is the exact rollback point"
data_loss_risk = false
requires_migration_down = false

[[feature.rfc-complete-oidc.interface]]
name = "Go relying-party API"
kind = "api"
stability = "unstable-unreleased"
change = "additive-with-protected-attempt-version-change"

[[feature.rfc-complete-oidc.verification]]
id = "verify"
kind = "unit-integration"
command = "make verify"
required = true
timeout_seconds = 900
side_effect = "local_write"
network = false
retries = 0

[[feature.rfc-complete-oidc.verification]]
id = "repeat-race"
kind = "stability"
command = "run go test -race ./... fifty consecutive times"
required = true
timeout_seconds = 1800
side_effect = "local_write"
network = false
retries = 0

[[feature.rfc-complete-oidc.verification]]
id = "fuzz"
kind = "fuzz"
command = "run each declared Fuzz target for five seconds"
required = true
timeout_seconds = 300
side_effect = "local_write"
network = false
retries = 0

[[feature.rfc-complete-oidc.verification]]
id = "clean-clone"
kind = "integration"
command = "make verify from a fresh local clone"
required = true
timeout_seconds = 900
side_effect = "local_write"
network = false
retries = 0

[[feature.rfc-complete-oidc.verification]]
id = "graph"
kind = "architecture"
command = "graphify extract --code-only and structural collision audit"
required = true
timeout_seconds = 300
side_effect = "external_read"
network = false
retries = 0

[feature.github-distribution]
id = "github-distribution"
slug = "github-distribution"
name = "Move public distribution identity to GitHub"
namespace = "distribution"
parent = ""
state = "done"
risk = "high"
assurance = "traceable-tests"
created_at = "2026-09-03T05:42:00Z"
activated_at = "2026-09-03T05:42:00Z"
updated_at = "2026-09-03T06:35:13Z"
completed_at = "2026-09-03T06:35:13Z"
folder = "workflow/features/github-distribution"
worktree = "/tmp/gotth-distribution-20260903/gotth-oidc"
branch = "feature/github-distribution"
scope = ["Move the Go module and repository-owned imports to github.com/gotthboard/gotth-oidc", "Document GitHub distribution and Forgejo development boundaries", "Prove clean public clone and import behavior"]
non_goals = ["Select a license", "Move or replace a tag", "Reverse the Forgejo-to-GitHub mirror", "Change repository ownership or account type"]
affected_files = ["README.md", "CONTRIBUTING.md", "SECURITY.md", "go.mod", "*.go", "cmd/**", "docs/**", "workflow.toml", "workflow/features/github-distribution/**"]
invariants = ["Forgejo remains canonical development", "GitHub receives refs only through the existing push mirror", "Runtime and public API behavior remain unchanged", "Existing tags remain immutable"]
acceptance = ["The exact old module path is absent outside canonical Forgejo URLs", "Format, tidy, vet, test, clean-clone, public import, mirror parity, and two cold Judge passes succeed", "License and account decisions remain explicitly blocked"]

[[feature.github-distribution.dependency]]
id = "rfc-complete-oidc"
type = "prerequisite"
state = "satisfied"
evidence = "workflow/features/rfc-complete-oidc/evidence/verification.md"
satisfied_at = "2026-09-03T05:42:00Z"
source_commit = "02a2b869b1331240dc52e23d116e6e1512a4d2ba"

[feature.github-distribution.rollback]
strategy = "revert_feature_commit"
notes = "Revert the migration commit; never rewrite existing tags or mirror history"
data_loss_risk = false
requires_migration_down = false

[[feature.github-distribution.interface]]
name = "Go module import path"
kind = "api"
stability = "unstable-unreleased"
change = "breaking-module-identity"

[[feature.github-distribution.verification]]
id = "local"
kind = "unit"
command = "go mod tidy, go vet -mod=readonly ./..., and go test -mod=readonly ./..."
required = true
timeout_seconds = 1200
side_effect = "local_write"
network = false
retries = 0

[[feature.github-distribution.verification]]
id = "public-import"
kind = "integration"
command = "clean clone and import from github.com/gotthboard/gotth-oidc"
required = true
timeout_seconds = 1200
side_effect = "external_read"
network = true
retries = 0

[[feature.github-distribution.verification]]
id = "mirror-parity"
kind = "integration"
command = "compare exact Forgejo and GitHub advertised heads and tags"
required = true
timeout_seconds = 300
side_effect = "external_read"
network = true
retries = 0
